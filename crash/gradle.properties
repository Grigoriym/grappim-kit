POM_ARTIFACT_ID=grappim-kit-crash
POM_NAME=grappim-kit crash
POM_DESCRIPTION=CrashReporter interface -- a crash-reporting seam a lower Gradle module can depend on without pulling in a concrete crash SDK, for Compose Multiplatform apps.
